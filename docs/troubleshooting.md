# Troubleshooting

## Product claims do not match the app

Compare landing copy and screenshots against `../ops` before editing the public page.

## Type errors appear after content edits

Run the configured static checks when allowed: `npm run typecheck` and `npm run lint`.

## Screenshots are stale

Update files under `public/screenshots/` and keep references in the page content consistent.
